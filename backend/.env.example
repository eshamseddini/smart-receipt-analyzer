DATABASE_URL=sqlite:///./smart_receipt.db
UPLOAD_DIR=uploads
OCR_ENGINE=tesseract
APP_ENV=development
MAX_UPLOAD_SIZE_MB=10

# n8n / Make integration: set WEBHOOK_ENABLED=true and point WEBHOOK_URL to
# your n8n (or Make) webhook trigger to get notified on every processed receipt.
WEBHOOK_ENABLED=false
WEBHOOK_URL=